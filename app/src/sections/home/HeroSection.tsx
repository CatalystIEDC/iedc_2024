import React from 'react'
import { spaceGrotesk } from '../../utils/fonts'

const HeroSection = () => {
  return (
    <div className={` ${spaceGrotesk.className}`}>HeroSection</div>
  )
}

export default HeroSection